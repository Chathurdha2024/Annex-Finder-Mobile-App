name: annex_app
description: A Flutter application for managing annexes.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  uuid: ^4.0.0
  http: ^1.2.0
  image_picker: ^1.0.0
  cached_network_image: ^3.3.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
